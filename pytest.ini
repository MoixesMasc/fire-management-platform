[pytest]
testpaths = services
asyncio_mode = auto
addopts =
    --tb=short
    --strict-markers
    -q
markers =
    integration: marks tests that require LocalStack or real AWS (deselect with -m "not integration")
